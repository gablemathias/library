const myLibrary = [];

function Book(author, title, pagesNumber, isRead = false) {
  this.author = author;
  this.title = title;
  this.pagesNumber = pagesNumber;
  this.isRead = isRead;
}

// function addBookToLibrary(book) {
  
// }